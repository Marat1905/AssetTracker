apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: assettracker-db-data
  name: assettracker-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
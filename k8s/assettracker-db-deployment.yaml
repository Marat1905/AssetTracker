apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: assettracker-db
  name: assettracker-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: assettracker-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: assettracker-db
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
        - env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: assettracker
            - name: POSTGRES_PASSWORD
              value: "12345678"
            - name: POSTGRES_USER
              value: postgresuser
          image: postgres:17-alpine
          name: assettracker-db
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data/pgdata
              name: assettracker-db-data
      restartPolicy: Always
      volumes:
        - name: assettracker-db-data
          persistentVolumeClaim:
            claimName: assettracker-db-data